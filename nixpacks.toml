[phases.setup]
commands = [
  "nix-env -iA nixpkgs.python310Full"
]

[phases.build]
commands = [
  "pip install -r backend/requirements.txt"
]

[phases.start]
commands = [
  "gunicorn backend.main:app"
]

[nixpkgs]
packages = [
  "python310Full"
]
